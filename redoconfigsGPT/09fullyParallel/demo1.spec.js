import { test } from "@playwright/test";

test("t6", async function ({ page }) {
  await page.waitForTimeout(4000);
});
test("t7", async function ({ page }) {
  await page.waitForTimeout(4000);
});
test("t8", async function ({ page }) {
  await page.waitForTimeout(4000);
});
test("t9", async function ({ page }) {
  await page.waitForTimeout(4000);
});
test("t10", async function ({ page }) {
  await page.waitForTimeout(4000);
});
